# identity-service

NestJS app placeholder. Implement authentication / Better Auth / sessions here.

```bash
# from repo root
pnpm install
pnpm dev:identity
```

Default listen: `http://0.0.0.0:4002` (override with `PORT` / `BIND_ADDRESS`).
